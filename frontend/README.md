This folder is a placeholder for the frontend application.

Recommended setup:

```
npx create-next-app@latest frontend
cd frontend
npm install axios ethers web3modal tailwindcss
```

Use `pages` or `app` routing for login, dashboards, and election flows.
